# Design docs

The founding set of spacer design docs lives under
[`origin/`](origin/README.md) and is frozen. New design work goes in
sibling directories under `design-docs/`, one per initiative, each
with its own `README.md`.
